Rating: <%= avg_rating %> <br/>
<% if user_signed_in? && rated? %>
Your rating: <%= user_rating %>
<% end %>
<% if user_signed_in? && !rated? && @pin.user_id != current_user.id %>
    <%= render 'ratings/form'%>
<% end %>